import SwiftUI

// Placeholder — implemented in a later phase
struct TimestampChipView: View {
    var body: some View {
        Text("0:00")
            .font(.caption.monospacedDigit())
    }
}

#Preview {
    TimestampChipView()
}
